spring:
  application:
    name: hr_platform

  datasource:
    url: jdbc:postgresql://ep-lively-poetry-abdxsmwk.eu-west-2.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_f9lEybma8Nsn
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      jakarta:
        persistence:
          validation:
            mode: callback

  messages:
    basename: messages

server:
  port: 8081
  error:
    include-message: always

# Twilio Configuration
twilio:
  account-sid: ${TWILIO_ACCOUNT_SID}
  auth-token: ${TWILIO_AUTH_TOKEN}
  whatsapp-from: ${TWILIO_WHATSAPP_FROM}

# JWT Configuration
application:
  security:
    jwt:
      secret-key: ${APPLICATION_SECURITY_JWT_SECRET_KEY}
      expiration: ${APPLICATION_SECURITY_JWT_EXPIRATION:86400000}